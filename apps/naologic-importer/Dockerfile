# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build naologic-importer`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t naologic-importer`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system naologic-importer && \
          adduser --system -G naologic-importer naologic-importer

COPY dist/apps/naologic-importer naologic-importer/
RUN chown -R naologic-importer:naologic-importer .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix naologic-importer --omit=dev -f install

CMD [ "node", "naologic-importer" ]
